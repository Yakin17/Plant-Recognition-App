name: plantrecognition_app
description: "A new Flutter project."

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.9.0


dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0

  cupertino_icons: ^1.0.8
  camera: ^0.11.2
  image_picker: ^1.2.0
  permission_handler: ^12.0.1
  image: ^4.5.4
  http: ^1.5.0
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^6.0.0


flutter:

  
  uses-material-design: true
  assets:
    - .env

  